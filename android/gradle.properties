# 1. C?u hình b? nh? cho Gradle và JVM
# T?ng RAM lên 2048MB và ??nh d?ng b?ng mã UTF-8
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8

# 2. C?u hình b? nh? riêng cho ti?n trình biên d?ch Kotlin (Tránh l?i Daemon terminated)
kotlin.daemon.jvmargs=-Xmx1024m

# 3. T?i ?u hóa t?c ?? build
# Cho phép ch?y song song các task n?u CPU có nhi?u nhân
org.gradle.parallel=true
# B?t tính n?ng l?u tr? k?t qu? (Cache) ?? l?n build sau nhanh h?n
org.gradle.caching=true
# Cho phép biên d?ch Kapt t?ng tr??ng (ch? biên d?ch ph?n thay ??i)
kapt.incremental.apt=true

# 4. Các c?u hình m?c ??nh c?a AndroidX và Kotlin
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# 5. Tùy ch?n: Gi?m t?i cho Kapt n?u b?n dùng Hilt/Room
kapt.use.worker.api=true